# pip install -r requirements.txt
# torch ships as a CUDA 12.8 wheel, so install it separately first:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128

torch==2.11.0
transformers==5.14.1
accelerate==1.14.0
safetensors==0.8.0
datasets==5.0.0
huggingface_hub==1.25.1

# evaluation harness
lm_eval==0.4.12
requests==2.34.2

# RULER synthesizes its samples at run time and needs these
wonderwords==3.0.1
nltk==3.10.0

# RULER's essay haystack (Paul Graham essays) is scraped as HTML
html2text==2025.4.15
